services:
  truyendex:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env.example
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    restart: unless-stopped
